Atomify Example
===============
 - Check out project: git clone https://github.com/almilo/atomify-example.git
 - Install npm dependencies: npm install
 - Install atomify globally: npm install -g atomify
 - Run atomify from the project folder: atomify
